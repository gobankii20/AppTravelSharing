<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- <corners android:radius="10dp" /> -->
    <corners
        android:bottomLeftRadius="@dimen/d20dp"
        android:bottomRightRadius="@dimen/d20dp"
        android:topLeftRadius="@dimen/d20dp"
        android:topRightRadius="@dimen/d20dp" />
    <solid android:color="@color/textColorWhite" />
</shape>